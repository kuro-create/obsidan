#claude

❯ 今回のやりとりを経て、最初にどのようなことを指示すればよかったか教ｔ

● 今回の流れを振り返ると、最初に以下を伝えていただくとスムーズでした。

  ---
  伝えておくとよかったこと

  1. 実行環境
  「Windows 11 上で動かしたい」
  「WSL2（Ubuntu）も使っている」
  → OS が分かれば最初から Windows 用フォント・エンコーディング設定で作れました。

  2. 日本語の表示・入力が必要
  「UI のラベル・ボタン・グラフはすべて日本語表示」
  「日本語をテキストとして入力できるようにしたい」
  → 最初から IME 対応・フォント設定を考慮した実装にできました。

  3. 配布・実行形式
  「最終的に .exe にして配布したい」
  「Python がない PC でも動かしたい」
  → PyInstaller を前提にした構成（単一ファイル、外部依存を最小化）で設計できました。

  ---
  理想的な最初の指示例

  ▎ Python で Windows 向けのレシート管理アプリを作りたいです。
  ▎ GUI は tkinter、日本語の表示と入力ができること。
  ▎ 機能は〇〇〇。最終的に .exe として配布したいです。

  この一文があれば、文字化け・IME・exe 化の問題を最初から回避した実装ができました。