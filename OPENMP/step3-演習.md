# **演習（あなたの理解を深めるための問題）**

以下のループが並列化できるか判定してください。 理由も書くと理解が深まります。

## **問題1**

c

```
for (int i = 0; i < n; i++) {
    a[i] = b[i] * 2;
}
```

## **問題2**

c

```
for (int i = 1; i < n; i++) {
    a[i] = a[i-1] + 1;
}
```

## **問題3**

c

```
int sum = 0;
for (int i = 0; i < n; i++) {
    sum += a[i];
}
```

## **問題4**

c

```
for (int i = 0; i < n; i++) {
    a[i] = b[n-i-1];
}
```

## **問題5**

c

```
for (int i = 0; i < n; i++) {
    a[i] = a[n-i-1];
}
```

孝汰さん、 **この5問を判定してみてください。**

あなたの回答を見て、 次に **parallel for の実装**に進みます。